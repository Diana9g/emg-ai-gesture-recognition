# Models

This folder is intended for trained EMG gesture recognition models.

Available model formats used in the project:
- `.keras`
- `.h5`

The repository includes the training scripts and project structure required to reproduce the workflow.
